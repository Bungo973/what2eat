# 项目 Agent 协作规范

本文件适用于仓库根目录及全部子目录。直接的系统、开发者和用户指令优先于本文件；子目录中的 `AGENTS.md` 可为其目录补充更具体的规则。

## 项目目标与边界

- 本项目交付「通用菜谱规划 Skill + 远程 MCP 服务 + 菜谱知识库」，产品依据为 `PRD/` 中最新版本的 PRD。
- Skill 面向支持类似能力封装的通用 Agent 宿主，不以 Codex 为唯一运行环境；宿主专用包装不得成为产品规则的唯一来源。
- 工程重点是 MCP 的稳定工具契约与可维护、可追溯的菜谱知识库；Skill 保持轻量，负责工作流指导和宿主适配。
- Agent/Skill 负责理解需求、最少量追问、约束权衡、工具编排和结果表达；MCP 负责事实、检索、标准化、确定性汇总与校验。
- 不在 MCP 中实现宽泛的自然语言方案生成器，不把未经审核的 AI 内容标记为已发布菜谱。
- P0 不扩展到消费者 Web 聊天产品、下单支付、社区投稿或精确营养医疗建议，除非用户明确修改范围。

## 开始工作前

1. 阅读本文件、PRD 中与任务相关的章节，以及 `cowork/` 下已有记录。
2. 检查工作区现状；这是多 Agent 共享目录，已有改动可能属于其他 Agent，不得覆盖、回滚或重写不在当前任务范围内的内容。
3. 明确本次任务边界和预计修改的文件。发现重叠编辑时，优先拆分文件或停止并说明冲突。
4. 技术栈、鉴权、数据格式等尚未定案的事项不得擅自固化；必要时以 ADR/规格提案记录备选与取舍。

## PRD 版本管理

- `PRD/` 中的历史版本只读保留。迭代 PRD 时必须新建更高版本文件，不得覆盖、改名或删除已有版本。
- 文件名延续 `AI原生菜谱Agent-PRD-V<major>.<minor>.md`；新版本应在文档信息中写明版本、日期、状态，并增加相对上一版的变更摘要。
- 实现默认以 `PRD/` 中版本号最高且状态可用于实施的文档为产品范围依据；若用户指定版本，则以指定版本为准。
- 发现旧版错误也不直接回改；在新版中说明修正内容，确需修订历史文件时必须得到用户明确授权。

## `cowork/` 变更记录（强制）

凡对仓库产生实质修改的 Agent，必须在同一轮工作中新增一份记录：

```text
cowork/YYYYMMDD-HHmm-<agent-id>-<short-topic>.md
```

- 使用 Asia/Shanghai 本地时间；`agent-id` 使用任务中可识别的名称，未知时使用 `agent-NNN`。
- 一次连续任务写一份记录。已有记录原则上只读；仅可继续补充自己尚未结束的记录，避免多 Agent 写同一文件。
- 记录必须包含：任务目标、修改文件及每个文件的修改意图、关键决定、验证命令与结果、遗留问题/下一步。
- 记录必须忠实反映最终状态；如果修改被撤销，也要注明。仅新增或更新本次 `cowork` 记录本身，无需递归记录。
- 未修改文件的纯分析任务不强制新增记录。

建议模板：

```markdown
# <主题>

- Agent：<agent-id>
- 时间：<ISO 8601，含时区>
- 状态：完成 | 部分完成 | 阻塞
- 任务：<目标>

## 修改

- `path/to/file`：<修改内容与原因>

## 决定

- <决定及理由；没有则写“无”>

## 验证

- `<command>`：通过/失败（简述结果）

## 遗留

- <风险、未决项或下一步；没有则写“无”>
```

## 设计与实现约束

### 单一事实源

- 平台无关的菜谱规划行为、数据契约和产物契约应放在 `specs/`；各 Agent 宿主的 Skill 或工作流包装只做适配和按需引用。
- 工具输入输出 schema 应由共享契约定义，MCP 实现和契约测试共同使用，避免复制后漂移。
- PRD 是产品范围依据；实现中发现歧义时，记录假设，不静默改变产品语义。

### Skill

- Skill 目录必须包含 `SKILL.md`，frontmatter 至少含准确、可区分的 `name` 和 `description`。
- `SKILL.md` 保持精简，放触发条件、关键流程、工具顺序、停止条件和行为边界；大段 schema、示例和专项规则放到按需读取的 `references/`。
- 核心规则不得依赖 Codex 专属指令、目录或调用语法。若某一宿主需要专用格式，应放在独立适配目录，并注明适用宿主。
- Skill 不保存动态菜谱、价格、凭证或其他运行时事实，也不直接连接价格平台。
- 默认允许自然语言匹配触发；只有用户明确要求时才改为仅显式调用。

### MCP 与领域层

- 工具保持窄、可组合、可验证；不得新增 `generate_plan(user_message)` 一类包办 Agent 推理的工具。
- 公共查询默认只返回 `published` 菜谱；进入方案的菜谱必须可追溯到 `recipe_id` 与确切 `version`。
- 已发布版本不可原地覆盖。归档不删除历史版本。
- 过敏原、忌口、状态和权限属于确定性硬过滤，不得被语义相似度或软排序绕过。
- 数量/单位无法可靠换算时保留原值并返回结构化警告，不伪造精确值；价格未知时返回未知，不以零代替。
- 公共只读能力与维护者写入能力分离；凭证不得写入代码、Skill、菜谱或日志。
- 工具错误需稳定、结构化并可判别，至少覆盖参数错误、未找到、冲突、未授权、限流、超时和数据源不可用。
- 对外工具名和 schema 一旦发布应保持向后兼容；破坏性变更必须显式版本化并记录迁移方案。

## 测试与验证

- 修改数据契约：运行 schema/fixture 校验和相关契约测试。
- 修改 MCP：运行单元测试、工具契约测试，并覆盖成功、无结果、非法输入、鉴权/限流及降级路径。
- 修改 Skill 或行为规范：运行 Skill 校验，并至少覆盖单次查询、周期规划、局部换菜三个 Agent 场景；不得只测试固定文案。
- 修改菜谱或食材数据：校验 ID/版本唯一性、单位、过敏原、步骤完整性和发布状态。
- 无法运行应有验证时，在 `cowork` 记录和最终回复中明确原因，不得写成“已通过”。

## 完成标准

- 修改仅覆盖当前任务，文件结构与命名一致，无占位内容或泄露的凭证。
- 相关测试或校验已执行，结果已核实。
- `cowork` 记录已新增并与最终修改一致。
- 最终交接说明完成内容、验证结果、风险和需要用户决定的事项。

## Git 与交付

- 预期 GitHub 仓库为 `https://github.com/Bungo973/what2eat`，配置后通常使用远端名 `origin`。
- 提交或推送前检查当前分支、工作区、提交范围和 `git remote -v`，不得把无关 Agent 的未完成改动混入提交。
- 未经用户明确要求，不创建提交、不推送、不改写远端历史；禁止强制推送。
- 首次配置远端时，如已有同名远端或地址不一致，停止并向用户说明，不擅自覆盖。
