# 定稿 PRD V0.2 并完成 Git 初始交付

- Agent：agent-001（首个 Agent）
- 时间：2026-08-23T21:29:14+08:00
- 状态：完成
- 任务：新建并定稿 PRD V0.2，初始化 Git 交付配置，创建首次提交并推送至用户指定的 GitHub 仓库。

## 修改

- `PRD/AI原生菜谱Agent-PRD-V0.2.md`：在保留 V0.1 的前提下新增定稿版本，明确通用 Skill 定位、MCP/知识库优先级、单版本单 Markdown 文档、六个公共工具、运行时索引与 grep 分工、黑盒输入输出、价格 provider、验收标准和实施顺序。
- `cowork/20260823-2129-agent-001-prd-v0.2-initial-push.md`：记录本轮定稿和 Git 初始交付。
- Git 仓库配置：初始分支由 `master` 改为 `main`，新增 `origin=https://github.com/Bungo973/what2eat.git`，仓库级提交身份设置为 `Bungo973 <Bungo973@users.noreply.github.com>`。

## 决定

- V0.1 保持不变，V0.2 作为可用于 P0 实施的定稿。
- 每个菜谱版本使用一份完整 Markdown 文档；YAML frontmatter 保存机器字段，正文保存做法和说明。
- 公共 MCP 工具确定为 `search_recipes`、`grep_recipe_docs`、`read_recipe`、`aggregate_shopping_list`、`validate_meal_plan`、`quote_ingredient_prices`。
- `normalize_ingredients` 作为 MCP 内部能力，不再单独暴露。
- P0 使用结构化运行时索引与受限正文搜索，不采用向量数据库。
- 用户未要求价格时不询问地区、不调用价格工具；未提出预算时默认不设预算上限。

## 验证

- PRD 结构检查：通过；V0.2 共 18 个顶层章节，文档状态为“定稿，可用于 P0 实施”。
- 工具契约检索：通过；六个公共工具均有独立规格。
- 占位内容检索：通过；未发现 `TODO`、`TBD`、“待定”或占位标记。
- `git remote -v`：通过；fetch/push 均指向用户指定仓库。
- `git ls-remote --symref origin HEAD`：通过；远端可连接，未返回现有 HEAD，符合空仓库状态。
- 初始提交与 `git push -u origin main`：通过。

## 遗留

- 尚未确定具体实现语言、MCP SDK、首发价格城市和种子菜谱数量；应作为实施前 ADR 决策。
- GitHub CLI 未安装；本次推送使用 Git 与现有凭证链路。
