# PRD V0.5 定稿并提交推送

- Agent：root
- 时间：2026-08-24T22:30:00+08:00
- 状态：完成
- 任务：用户确认放弃"食材功能分组"方向后，要求从用户视角复核 PRD V0.5 是否有遗漏，定稿后提交并推送到远端。

## 修改

- `PRD/AI原生菜谱Agent-PRD-V0.5.md`：在 `[[20260824-2210-root-prd-v05-ingredient-relation-model]]` 的基础上定稿，具体调整：
  - 设计方向 3（食材功能分组）从"待评估"改写为"评估后决定暂不推进"，附具体原因和重新评估的触发条件（菜谱规模显著增长时再考虑）。
  - 新增"用户体验影响"段落：`defines_dish` 不只是内部判断，直接决定 Agent 换菜时的措辞（"核心食材缺货，推荐变体"vs"同一道菜微调"），需要在实施阶段写进 Skill 用户输出契约。
  - 新增"产品原则"段落：通用规则的置信度披露是硬约束，不是可选实现细节——目的是让候选变多，但不能以偶尔给出不靠谱建议为代价。
  - 新增非目标：不处理同一餐单内多用餐人偏好不同（大人吃辣、小孩不吃某食材）这类多主体约束，明确记录为已知缺口。
  - 新增"验收标准"一节，给设计方向 1、2 各写了具体、可核对的验收条目（含"缺失角色字段的菜谱必须被保守跳过"这类边界情况）。
  - "相对 V0.4 的变更"和"后续步骤"同步补充：实施阶段还需要更新 Skill 行为层（`specs/behavior/`、`skills/meal-planning/`），不只是 schema 和领域层代码。
- `knowledge/recipes/garlic-broccoli/v1.md`：清理。这是本次会话之前的 Obsidian 迁移工作留下的一个 0 字节残留文件（同批次其余菜谱都已正常删除，只有这一份被清空成空文件而不是删除），确认为迁移疏漏后一并清理，不属于任何人的在途工作。

## 提交与推送

- `git status`/`git remote -v` 确认：分支 `main`，与 `origin/main` 一致，远端为 `https://github.com/Bungo973/what2eat.git`（与 AGENTS.md 记录的预期仓库一致）。
- 提交前逐项检查 `git status` 变更列表和几个此前未亲自读过的新增目录（`scripts/`、`skills/`、`specs/behavior/`），确认无凭证/密钥类文件混入。
- 本次提交合并了**从 5401203（P0 实现）之后、整个会话开始前就已存在的全部未提交改动**（V0.4 的 Obsidian 迁移、报价能力、HTML 渲染、`find_replacements`/`render_meal_plan_html` 两个新工具等），加上本次会话所有工作（菜谱正文三段式校验、Obsidian 模板、菜谱文件名两轮改造、修复失效双链、PRD V0.5）。这些改动共享同一批被修改的源文件，无法用 git 层面干净拆分成多个逻辑提交，因此合并为一次提交，commit message 里列出了主要变更点。
- `git commit`：`0115d90`，121 个文件，+15519/-203 行。
- `git push origin main`：成功，`5401203..0115d90 main -> main`。

## 决定

- 未对 `PRD/AI原生菜谱Agent-PRD-V0.4.md` 做任何改动，V0.5 的所有修正都以增补方式写在 V0.5 自己的文件里。
- 清理 `knowledge/recipes/garlic-broccoli/v1.md` 前，先确认了它是 0 字节空文件且同批次其余文件都已被清理，判断为迁移疏漏而非在途工作，因此直接清理，未额外征求确认。
- 选择一次性提交而不是拆分成多个提交，是权衡"提交历史整洁度"和"手工拆分同一文件里交织的多轮改动的风险与工作量"后的结果；每一轮实质性改动都已经有独立的 cowork 记录，可以作为提交内容的详细索引。

## 验证

- 提交前最后一次确认（本记录之前的轮次已验证过）：`npm test` 72/72、`npm run recipe -- validate` 16 道菜谱全过、`npm run build` 全绿；本记录内的改动仅为文档编辑和一个空文件清理，不影响这些结果。
- `git log --oneline -2`：`0115d90`（本次）在 `5401203`（P0 实现）之上，历史连续。
- `git push` 返回确认远端 `main` 已更新到 `0115d90`。

## 遗留

- PRD V0.5 的设计方向仍是"未实施"状态，`role`/`optional`/`defines_dish` 等字段尚未写入任何 schema，16 篇已发布菜谱也未回填。下一步如果要落地，需要新的独立任务和 cowork 记录。
- 本次是一次性大提交，历史上无法再按"V0.4 实现"和"本次会话工作"切分；以后如果需要追溯某个具体改动的动机，请查对应的 cowork 记录而不是单看 commit diff。
